# Copy to .env and fill in. The agent layer needs at least one of these.
# Without either, `python cli.py lab --offline` still runs the built-in panel,
# and the engine, calibration and validation work with no key at all.

KIE_API_KEY=
OPENROUTER_API_KEY=
